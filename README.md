To run this, run the following commands:

1. npm install
2. bower install
3. node server.js

then visit localhost:3000 in your browser (from two browsers), and you will see the drawing synced and the chat part sync
